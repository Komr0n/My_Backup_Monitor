<Window x:Class="BackupMonitor.Views.PeriodResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Результат проверки периода" Height="340" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize"
        Background="{DynamicResource WindowBackground}"
        FontFamily="{StaticResource FontFamilyDefault}"
        FontSize="{StaticResource FontSizeNormal}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Name="HeaderText" FontWeight="SemiBold" FontSize="{StaticResource FontSizeMedium}"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="1" Name="StatusText" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,10"/>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Отсутствующие даты:" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                    <ItemsControl Name="MissingList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Закрыть" Width="100"
                    Style="{StaticResource PrimaryButtonStyle}" IsDefault="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
