<Window x:Class="BackupMonitor.Views.PeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Проверка за период" Height="251" Width="443"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Дата начала:" Margin="0,0,0,5"/>
        <DatePicker Name="DpStartDate" Grid.Row="1" Margin="0,0,0,15"/>

        <Label Grid.Row="2" Content="Дата окончания:" Margin="0,0,0,5"/>
        <DatePicker Name="DpEndDate" Grid.Row="3" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="BtnOk" Content="OK" Width="100" Height="30" Margin="0,0,10,0" Click="BtnOk_Click" IsDefault="True"/>
            <Button Name="BtnCancel" Content="Отмена" Width="100" Height="30" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

