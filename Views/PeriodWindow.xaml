<Window x:Class="BackupMonitor.Views.PeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Проверка за период" Height="260" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        FontFamily="{StaticResource FontFamilyDefault}"
        FontSize="{StaticResource FontSizeNormal}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Дата начала:" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,5"/>
        <DatePicker Name="DpStartDate" Grid.Row="1" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="2" Text="Дата окончания:" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,5"/>
        <DatePicker Name="DpEndDate" Grid.Row="3" Margin="0,0,0,14"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Name="BtnOk" Content="✔ OK" Width="100"
                    Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Click="BtnOk_Click" IsDefault="True"/>
            <Button Name="BtnCancel" Content="Отмена" Width="100"
                    Style="{StaticResource SecondaryButtonStyle}" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
